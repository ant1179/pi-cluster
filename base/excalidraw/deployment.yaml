apiVersion: apps/v1
kind: Deployment
metadata:
  name: excalidraw
spec:
  replicas: 1

  selector:
    matchLabels:
      app: excalidraw

  template:
    metadata:
      labels:
        app: excalidraw

    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault

      containers:
        - name: excalidraw
          image: excalidraw/excalidraw:latest

          ports:
            - name: http
              containerPort: 8080
              protocol: TCP

          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 101
            runAsGroup: 101 # default nginx user
            capabilities:
              drop:
                - NET_RAW
                - MKNOD
                - AUDIT_WRITE
                - SETFCAP
                # - CHOWN
                - FOWNER
                - FSETID
                - KILL
                # - SETGID
                # - SETUID
                - DAC_OVERRIDE
                - NET_BIND_SERVICE

          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi

          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 2
            failureThreshold: 3

          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
            - name: nginx-cache
              mountPath: /var/cache/nginx

      volumes:
        - name: nginx-config
          configMap:
            name: excalidraw-nginx
        - name: nginx-cache
          emptyDir: {}
