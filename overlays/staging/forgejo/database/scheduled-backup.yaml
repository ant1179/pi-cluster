apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: forgejo-daily-backup
  namespace: forgejo-staging
spec:
  schedule: "0 3 * * *"
  backupOwnerReference: cluster
  cluster:
    name: forgejo-postgres-cnpg
