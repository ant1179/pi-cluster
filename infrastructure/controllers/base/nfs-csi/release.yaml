apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: csi-driver-nfs
  namespace: nfs-csi
spec:
  interval: 30m
  targetNamespace: nfs-csi
  chart:
    spec:
      chart: csi-driver-nfs
      version: 4.13.2
      sourceRef:
        kind: HelmRepository
        name: csi-driver-nfs
        namespace: flux-system
  values:
    driver:
      name: nfs.csi.k8s.io
